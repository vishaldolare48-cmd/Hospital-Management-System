{
  "cleanUrls": true,
  "buildCommand": "cd hms-frontend && npm install && npm run build",
  "outputDirectory": "hms-frontend/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
